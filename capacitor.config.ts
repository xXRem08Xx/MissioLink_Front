import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.missiolink.apptest',
  appName: 'MissioLink',
  webDir: 'dist/test/browser'
};

export default config;
