import { Routes } from '@angular/router';
import { MissionCandidatesComponent } from './mission-candidates.component';

export const MISSIONS_CANDIDATES_ROUTES: Routes = [
  { path: '', component: MissionCandidatesComponent },
];
