import { Routes } from '@angular/router';
import { MissionDetailComponent } from './mission-detail.component';

export const MISSION_DETAIL_ROUTES: Routes = [
  { path: '', component: MissionDetailComponent }
];
