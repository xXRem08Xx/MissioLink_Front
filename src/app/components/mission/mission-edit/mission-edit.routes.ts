import { Routes } from '@angular/router';
import { MissionEditComponent } from './mission-edit.component';

export const MISSION_EDIT_ROUTES: Routes = [
  { path: '', component: MissionEditComponent }
];
